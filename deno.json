{
  "tasks": {
    "dev:server": "deno install && deno run --watch --allow-all main.ts",
    "dev:ui": "cd ui && deno install && deno run dev",
    "build:ui": "cd ui && deno install && deno run build",
    "serve": "deno run build && deno run --allow-all main.ts"
  },
  "nodeModulesDir": "auto",
  "imports": {
    "@oak/oak": "jsr:@oak/oak@^17.2.0",
    "@std/assert": "jsr:@std/assert@1",
    "@std/path": "jsr:@std/path@^1.1.4"
  }
}
